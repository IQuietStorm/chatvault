{
  "name": "@chatvault/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Versioned WebSocket envelope + payload contracts shared by server and clients",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
